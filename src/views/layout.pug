doctype 5
html
    head
        title= "RMBER " + title
        script(src='/src/public/minified/scripts.min.js')
        link(rel='stylesheet', href='/src/public/minified/styles.min.css')
    body
        .container-fluid.navbar.navbar-default
            .navbar-header
                button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href= online ? '/api/user/docs' : '/docs')
                    | RMBER
            .collapse.navbar-collapse
                ul.nav.navbar-nav.navbar-right
                    li(class=homeNav ? 'active' : '')
                        a(href='/')
                            | Home
                    li(class=docsNav ? 'active' : '')
                        a(href='/docs')
                            | Docs
                    li(class=bugsNav ? 'active' : '')
                        a(href='/bugs')
                            | Bug Reports
    block content
